{
  "$defs": {
    "Definition": {
      "additionalProperties": false,
      "anyOf": [
        {
          "required": ["registries"]
        },
        {
          "required": ["scheduled_tasks"]
        },
        {
          "required": ["services"]
        },
        {
          "required": ["script"]
        }
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "registries": {
          "default": [],
          "items": {
            "$ref": "#/$defs/Registry"
          },
          "type": "array"
        },
        "scheduled_tasks": {
          "default": [],
          "items": {
            "$ref": "#/$defs/ScheduledTask"
          },
          "type": "array"
        },
        "services": {
          "default": [],
          "items": {
            "$ref": "#/$defs/Service"
          },
          "type": "array"
        },
        "script": {
          "$ref": "#/$defs/Script",
          "default": {
            "apply": null,
            "revert": null
          }
        }
      },
      "required": ["id", "name", "description"],
      "type": "object"
    },
    "NotExistType": {
      "const": "<NotExist>",
      "type": "string"
    },
    "Registry": {
      "properties": {
        "path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "type": {
          "$ref": "#/$defs/RegistryValueKind"
        },
        "old_value": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/NotExistType"
            }
          ]
        },
        "new_value": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/NotExistType"
            }
          ]
        }
      },
      "required": ["path", "name", "type", "old_value", "new_value"],
      "type": "object"
    },
    "RegistryValueKind": {
      "enum": [
        "String",
        "ExpandString",
        "Binary",
        "DWord",
        "MultiString",
        "QWord"
      ],
      "type": "string"
    },
    "ScheduledTask": {
      "properties": {
        "full_path": {
          "type": "string"
        },
        "old_state": {
          "$ref": "#/$defs/ScheduledTaskState"
        },
        "new_state": {
          "$ref": "#/$defs/ScheduledTaskState"
        }
      },
      "required": ["full_path", "old_state", "new_state"],
      "type": "object"
    },
    "ScheduledTaskState": {
      "enum": ["Enabled", "Disabled"],
      "type": "string"
    },
    "Script": {
      "properties": {
        "apply": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "revert": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": ["apply", "revert"],
      "type": "object"
    },
    "Service": {
      "properties": {
        "name": {
          "type": "string"
        },
        "old_startup_type": {
          "$ref": "#/$defs/ServiceStartupType"
        },
        "new_startup_type": {
          "$ref": "#/$defs/ServiceStartupType"
        }
      },
      "required": ["name", "old_startup_type", "new_startup_type"],
      "type": "object"
    },
    "ServiceStartupType": {
      "enum": [
        "Automatic",
        "AutomaticDelayedStart",
        "Disabled",
        "InvalidValue",
        "Manual"
      ],
      "type": "string"
    }
  },
  "properties": {
    "definitions": {
      "default": [],
      "items": {
        "$ref": "#/$defs/Definition"
      },
      "type": "array"
    },
    "preload": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    }
  },
  "type": "object"
}
